# Dev / test dependencies — NOT installed in the production Docker image.
# Install with: pip install -r requirements-dev.txt
-r requirements.txt

# Testing
pytest>=8.0.0
httpx>=0.27.0

# Code quality
black>=24.1.1
isort>=5.13.2
flake8>=7.0.0
mypy>=1.8.0

# Data tools (used in scripts / notebooks only)
pandas>=2.2.0
numpy>=1.26.0
jq>=1.6.0
typer>=0.9.0
